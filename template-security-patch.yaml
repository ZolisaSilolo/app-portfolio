# Add these resources to your existing template.yaml

  # Usage Plan for rate limiting
  ApiUsagePlan:
    Type: AWS::ApiGateway::UsagePlan
    Properties:
      UsagePlanName: !Sub "${AWS::StackName}-usage-plan"
      Description: Rate limiting for portfolio API
      Throttle:
        RateLimit: 100    # requests per second
        BurstLimit: 200   # burst capacity
      Quota:
        Limit: 10000      # requests per day
        Period: DAY
      ApiStages:
        - ApiId: !Ref ServerlessRestApi
          Stage: !Ref ServerlessRestApiProdStage

  # API Key (optional - for monitoring)
  ApiKey:
    Type: AWS::ApiGateway::ApiKey
    Properties:
      Name: !Sub "${AWS::StackName}-api-key"
      Description: API key for portfolio
      Enabled: true

  # Link usage plan to API key
  UsagePlanKey:
    Type: AWS::ApiGateway::UsagePlanKey
    Properties:
      KeyId: !Ref ApiKey
      KeyType: API_KEY
      UsagePlanId: !Ref ApiUsagePlan

  # Add to your Lambda function properties:
  # Events:
  #   Api:
  #     Type: Api
  #     Properties:
  #       Path: /your-path
  #       Method: post
  #       Auth:
  #         ApiKeyRequired: false  # Keep false for public portfolio
